version: '3.3'

services:
  nuxt:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    image: nuxt_dev
    container_name: nuxt-dev
    command: yarn dev
    volumes:
      - ./pages:/app/pages/
      - ./assets:/app/assets/
    ports:
      - '3000:3000'
